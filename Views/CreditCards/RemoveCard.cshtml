@model CardPortfolio.Models.CreditCard

@{
    ViewData["Title"] = "RemoveCard";
}

<h1>RemoveCard</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>@ViewBag.Title</h4>

    
    <form asp-action="RemoveCard">
        <input type="submit" value="Remove Card" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
